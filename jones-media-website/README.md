# Deprecated Static Copy

This directory is intentionally deprecated.

Single source of truth:
- `app/` (React app)

Policy:
- Do not implement or review UI changes in this folder.
- Make all product and design changes in `app/src` only.
- If a static export is needed, generate it from the React app build pipeline.
